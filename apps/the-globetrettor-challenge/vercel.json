{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && yarn install --frozen-lockfile && NODE_ENV=production yarn nx run the-globetrettor-challenge:build:production --outputPath=/vercel/path0/.next",
  "installCommand": "cd ../.. && yarn install --frozen-lockfile",
  "outputDirectory": ".next"
}
