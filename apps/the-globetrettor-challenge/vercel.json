{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && yarn install --frozen-lockfile && yarn nx build the-globetrettor-challenge --prod",
  "installCommand": "cd ../.. && yarn install --frozen-lockfile",
  "outputDirectory": ".next"
}
